services:
  mongo:
    image: mongo:7
    container_name: gila-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - gila-mongo-data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.runCommand({ ping: 1 })"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  gila-mongo-data:
