{
  "name": "gila-community",
  "version": "1.0.0",
  "private": true,
  "description": "gila community — PostIt-style social app with gila design system",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "start": "NODE_ENV=production npm run start --prefix server",
    "atlas:bootstrap": "bash scripts/atlas-bootstrap.sh",
    "make-admin": "npm run make-admin --prefix server --"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
